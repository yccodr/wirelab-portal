{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["check-types", "^build"],
      "outputs": ["dist"]
    },
    "check-types": {},
    "lint": {},
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
